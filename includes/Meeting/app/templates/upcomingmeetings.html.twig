{% extends 'banner.html.twig' %}
{% block content %}
<html lang="en">
<head>
    <meta name = "viewport" content = "width=device-width, initial-scale = 1.0">


    <title></title>

</head>

<div class = "menu-list">
    <ul>
        <li><a href="{{ landing_page }}">Home</a></li>
        <li><a href="{{ upcoming_meetings }}">Scheduled Meetings</a></li>
        <li><a href="{{ meeting_requests }}">New Meeting Requests</a></li>
        <li><a href="{{ hosted_meetings }}">Meetings hosted</a></li>
        <li><a href="{{ edit_profile }}">Account settings</a></li>
        <li><a href="{{ create_event }}">Create event</a></li>
        <li><a href="{{ view_event }}">View event</a></li>
    </ul>
</div>
<body>

<div id="page-content-div">

        {% for code, name in upcomingMeetings %}
            <h4 value="{{code}}">

                Subject: {{ name[5] }}<br> Host: {{name[3]}} <br> On: {{name[1]}} <br> At: {{name[0]}} <br> For: {{ name[2] }} minutes </h4>
            <a href='meetingnotes?MiD={{ name[6] }}'>
                <img src="../media/on.png"
                     alt="turn off meeting" width="70" height="20">
        {% endfor %}
    <br>
        {% for code, name in repeatMeetings1 %}
            <h4 value="{{code}}"> Subject: {{ name[4] }}<br> Repeats: {{name[3]}} ({{name[1]}}) <br>  For: {{ name[2] }} minutes at: {{ name[5] }} </h4>
            <a href='meetingnotes?MiD={{ name[0] }}'>
                 <img src="../media/on.png"
                    alt="turn off meeting" width="70" height="20">
        {% endfor %}
        {% for code, name in repeatMeetings2 %}
            <h4 value="{{code}}"> Subject: {{ name[4] }}<br> Repeats: {{name[3]}} ({{name[1]}}) <br>  For: {{ name[2] }} minutes at: {{ name[5] }} </h4>
                <a href='meetingnotes?MiD={{ name[0] }}'>
                    <img src="../media/on.png"
                         alt="turn off meeting" width="70" height="20">
                    {% endfor %}
{#    <br>#}
        {% for code, name in pastMeetings %}
         <h4 value="{{code}}"> Subject: {{ name[5] }}<br> Host: {{name[3]}} <br> On: {{name[1]}} <br> At: {{name[0]}} <br> For: {{ name[2] }} minutes </h4>
          <a href='meetingnotes?MiD={{ name[6] }}'>
           <img src="../media/on.png"
            alt="turn off meeting" width="70" height="20">
            {% endfor %}
</div>
</body>
</html>
    <script>
        var coll = document.getElementsByClassName("collapsible");
        var i;

        for (i = 0; i < coll.length; i++) {
            coll[i].addEventListener("click", function() {
                this.classList.toggle("active");
                var content = this.nextElementSibling;
                if (content.style.display === "block") {
                    content.style.display = "none";
                } else {
                    content.style.display = "block";
                }
            });
        }
    </script>
{% endblock %}